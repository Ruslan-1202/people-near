package ru.practicum.peoplenear.validation;

/**
 * Группы валидации, не очень нужно в этом проекте, не слишком удобно, в рамках эксперимента
 */
public interface Marker {
    interface onCreate {
    }

    interface onUpdate {
    }
}
